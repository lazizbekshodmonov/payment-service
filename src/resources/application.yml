server:
  port: 3000

database:
  postgres:
    host: {POSTGRES_HOST}
    port: {POSTGRES_PORT}
    username: {POSTGRES_USER}
    password: {POSTGRES_PASSWORD}
    name: {POSTGRES_DB}
  redis:
    host: {POSTGRES_HOST}
    port: {POSTGRES_PORT}
    username: {POSTGRES_USER}
    password: {POSTGRES_PASSWORD}
    name: {POSTGRES_DB}

security:
  jwt_secret: {JWT_SECRET}

providers:
  plum:
    active: false
    production: false
    host: https://checkout.paycom.uz
    merchant_id: 1
